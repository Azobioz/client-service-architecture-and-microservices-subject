server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://order-db:5432/order_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true